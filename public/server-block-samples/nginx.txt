root /path/to/savo-project/public;
index index.php;

location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ~* \.(jpg|jpeg|png|gif|webp|svg|ico|css|js|woff2)$ {
    expires 30d;
    add_header Cache-Control "public, no-transform";
}